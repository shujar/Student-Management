import { TrimLeadingWhitespaceDirective } from './trim-leading-whitespace.directive';

describe('TrimLeadingWhitespaceDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimLeadingWhitespaceDirective();
    expect(directive).toBeTruthy();
  });
});
